class playersData {
  final Map player;
  final String uid;
  final String role;
  final String word;
  final String response;
  final bool roundFinished;
  final int points;

  playersData(this.uid, this.role, this.word, this.response, this.player,this.roundFinished, this.points);
}
